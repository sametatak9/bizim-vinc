# Archived / experimental migrations

These dated files under `supabase/migrations/` were exploratory.

**Do not use for new environments.** Canonical ordered migrations live in `/migrations` (`0002`–`0011`).

See `docs/DATABASE.md`.
